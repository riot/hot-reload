# Riot Hot Module Replacement

Watch Riot tag files, when changed: compile and update live

## Install

* `git clone https://github.com/riot/hmr.git`
* `cd hmr`
* `npm install`

## Run

* `npm start`
